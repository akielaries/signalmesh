# host update tools version history. newest entry FIRST - auto-released on push to
# main as tag tools/v<version>, publishing update.bin + mkupdate.bin.
releases:
  - version: 0.1.0
    notes:
      - initial host tools release
      - update.bin (uploader) and mkupdate.bin (image wrapper)
